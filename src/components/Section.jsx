const Section = ({children}) => {
    return (
        <section>
            <div className="section-body">
                    {children}
            </div>
        </section>
    )
}
export default Section